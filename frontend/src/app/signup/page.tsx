'use client';
import { withAuthRedirect } from '@/components/withAuthRedirect';

function SignupPage() {
  return (
    <div>
      <h1>Sign Up</h1>
      {/* Signup form */}
    </div>
  );
}

export default withAuthRedirect(SignupPage); 